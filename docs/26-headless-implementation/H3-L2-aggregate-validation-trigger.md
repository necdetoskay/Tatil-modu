# H3 / L2 Aggregate Validation Trigger

This file exists to trigger the aggregate H3/L2 validation workflow from the latest `main` baseline after capability gateway, deterministic mock provider, registry, retry/timeout policy, full fault injection catalog, and canonical capability coverage were implemented.

H4 remains locked until this aggregate run passes with zero P0 failures.
