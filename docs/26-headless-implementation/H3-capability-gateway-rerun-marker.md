# H3 Capability Gateway Clean Rerun

This marker triggers a clean PR validation from the latest `main` after aligning the provider boundary and tests with the normalized `unknown` transport contract.
