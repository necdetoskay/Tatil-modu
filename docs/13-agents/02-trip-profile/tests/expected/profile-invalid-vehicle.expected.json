{
  "fixtureId": "profile-invalid-vehicle",
  "testType": "adversarial",
  "expected": {
    "conflictFlags": ["vehicle_invalid"],
    "vehicle": {
      "type": "any",
      "note": "Geçersiz enum → any'ye normalize edilir"
    }
  },
  "checks": [
    "CF-06 triggered: vehicle.type = 'uçak' geçersiz enum",
    "conflictFlags contains 'vehicle_invalid'",
    "vehicle.type normalized to 'any' (fallback)",
    "confidence.score <= 0.7 (rule violation penalty)"
  ]
}
